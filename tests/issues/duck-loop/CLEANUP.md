# Cleanup

Disposable fixture directories:

- `tests/issues/duck-loop/sprint1`
- `tests/issues/duck-loop/sprint2`
- `tests/issues/duck-loop/sprint3`

Remove this disposable test folder with:

```sh
rm -rf tests/issues/duck-loop
```
