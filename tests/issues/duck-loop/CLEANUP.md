# Cleanup

This fixture directory is disposable.

Sprint directories currently included:

- `sprint1`
- `sprint2`
- `sprint3`

From the repository root, remove it with:

```bash
rm -rf tests/issues/duck-loop
```
