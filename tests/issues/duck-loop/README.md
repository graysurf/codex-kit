# Duck Loop Root (Disposable Test Fixture)

This directory is test-only and exists for the plan-issue-delivery-loop duck test in Sprint 1 Task S1T1.

Do not treat anything under `tests/issues/duck-loop` as production or long-lived project content.
This fixture root is intentionally disposable and may be removed after validation.

See `tests/issues/duck-loop/CLEANUP.md` for the removal command.
