#!/usr/bin/env bash
set -euo pipefail

log_dir="${CODEX_STUB_LOG_DIR:-}"
if [[ -n "$log_dir" ]]; then
  mkdir -p "$log_dir"
  printf "%s\n" "terminal-notifier $*" >"${log_dir%/}/backend.args.txt"
fi

exit 0

