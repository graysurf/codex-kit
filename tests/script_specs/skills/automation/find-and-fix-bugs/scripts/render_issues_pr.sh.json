{
  "smoke": [
    {
      "name": "issues-template",
      "args": ["--issues"],
      "timeout_sec": 5,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "^# Issues List$"
      }
    },
    {
      "name": "pr-template",
      "args": ["--pr"],
      "timeout_sec": 5,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "^## Issues Found$"
      }
    }
  ]
}

