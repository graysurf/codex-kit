{
  "smoke": [
    {
      "name": "fixture-last",
      "args": ["--file", "tests/fixtures/rest/.rest_history", "--last"],
      "timeout_sec": 5,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "POST /login"
      }
    }
  ]
}
