{
  "smoke": [
    {
      "name": "tun-mssql",
      "command": [
        "zsh",
        "-f",
        "-c",
        "codex_mssql_run() { emulate -L zsh; setopt localoptions nounset; local prefix=$1 env_file=$2; shift 2; print -r -- prefix=$prefix; print -r -- env_file=${env_file:t}; print -r -- args=$*; }; source skills/_projects/tun-group/scripts/tun-mssql.zsh; tun-mssql --help"
      ],
      "timeout_sec": 10,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "prefix=TUN\\nenv_file=tun-mssql\\.env\\nargs=--help"
      }
    }
  ]
}
