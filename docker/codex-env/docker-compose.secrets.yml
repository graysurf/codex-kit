services:
  codex-env:
    environment:
      GH_TOKEN: ${GH_TOKEN:-}
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
    volumes:
      - ${CODEX_SECRET_DIR_HOST:?Set CODEX_SECRET_DIR_HOST}:/opt/zsh-kit/scripts/_features/codex/secrets:rw
