---
description: Run fr-psql with provided args
argument-hint: psql args or SQL
---

If fr-psql is not already available, source it first:
source ~/.codex/tools/fr-psql/fr-psql.zsh

Then run:
fr-psql $ARGUMENTS

If $ARGUMENTS is empty, ask for SQL or flags.
