---
description: Run qb-mysql with provided args
argument-hint: mysql args or SQL
---

If qb-mysql is not already available, source it first:
source ~/.codex/tools/qb-mysql/qb-mysql.zsh

Then run:
qb-mysql $ARGUMENTS

If $ARGUMENTS is empty, ask for SQL or flags.
