---
description: Run qbmysql with provided args
argument-hint: mysql args or SQL
---

If qbmysql is not already available, source it first:
source ~/.codex/tools/qbmysql/qbmysql.zsh

Then run:
qbmysql $ARGUMENTS

If $ARGUMENTS is empty, ask for SQL or flags.
