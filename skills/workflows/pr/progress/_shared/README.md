# skills/workflows/pr/progress/_shared

Purpose: shared assets for progress PR workflow skills.

Allowed contents:

- `lib/` (shared workflow helpers)
- `references/` (workflow docs/specs)
- `assets/` (templates, fixtures)
- `python/` (language-specific helpers)

Forbidden:

- `SKILL.md`
- `scripts/`
- `tests/`

Keep entrypoints in per-skill `scripts/` directories.
