
Please address the following on the existing assigned branch/worktree/PR lane before merge:

1. `<required change>`
2. `<required change>`
3. `<test coverage / validation update>`

If required context is missing or conflicting, stop and reply in this PR thread with:

- Confirmed task-lane facts (`Owner / Branch / Worktree / PR`)
- Exact missing/conflicting input
- Next unblock action needed

When done, reply in this PR thread with concrete evidence links and validation results.
