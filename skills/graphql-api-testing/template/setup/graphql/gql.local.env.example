# GraphQL API Testing (Codex skill) - optional runtime env toggles (DO NOT COMMIT).
#
# Copy to:
#   setup/graphql/gql.local.env
#
# Then load it in your shell (pick one):
#   - source setup/graphql/gql.local.env
#   - direnv (.envrc): source_env setup/graphql/gql.local.env
#
# Notes:
# - This file is NOT read automatically by gql.sh/gql-report.sh.
# - It is gitignored by the template rule: setup/graphql/*.local.env

# ----------------------------
# Endpoint / auth overrides
# ----------------------------
#
# Prefer project presets:
#   - setup/graphql/endpoints.env (+ optional endpoints.local.env)
#   - setup/graphql/jwts.env (+ optional jwts.local.env)
#
# But you can still override at runtime:

# export GQL_URL="http://localhost:6700/graphql"
# export GQL_JWT_NAME="default"
# export ACCESS_TOKEN="<bearer token>"

# ----------------------------
# Command history (gql.sh)
# ----------------------------

# Enable/disable writing setup/graphql/.gql_history (default: 1)
export GQL_HISTORY=1

# Include URL value in history entries when using --url/GQL_URL (default: 1)
export GQL_HISTORY_LOG_URL=1

# Rotate when history file exceeds N MB (default: 10; 0 disables)
export GQL_HISTORY_MAX_MB=10

# Number of rotated history files to keep (default: 5)
export GQL_HISTORY_ROTATE_COUNT=5

# Override history file path:
# - absolute path: /abs/path/.gql_history
# - relative path: resolved under setup/graphql/
# export GQL_HISTORY_FILE=".gql_history"

# ----------------------------
# Reporting (gql-report.sh)
# ----------------------------

# Default report output directory when --out is not set.
# If relative, it is resolved against <project root>. (default: docs)
# export GQL_REPORT_DIR="docs"

# Include the `gql.sh` command snippet in the report (default: 1)
export GQL_REPORT_INCLUDE_COMMAND=1

# Include URL value in the report command snippet when --url is used (default: 1)
export GQL_REPORT_COMMAND_LOG_URL=1

# Allow generating a report without a real / non-empty response (default: 0)
# export GQL_ALLOW_EMPTY=0
