# JWT presets (no secrets in this file).
#
# Put real tokens in `setup/graphql/jwts.local.env` (gitignored via setup/graphql/*.local.env).
# Select which one to use:
#   - CLI: api-gql call --jwt <name> ... (from `brew install nils-cli`)
#   - Env: export GQL_JWT_NAME="<name>"
#   - Or (local-only): set `GQL_JWT_NAME=<name>` inside `setup/graphql/jwts.local.env`
#
# Naming convention:
#   GQL_JWT_DEFAULT   -> name "default"
#   GQL_JWT_ADMIN     -> name "admin"
#   GQL_JWT_MEMBER    -> name "member"
#
# If the selected JWT is missing/empty, api-gql will fall back to running `setup/graphql/operations/login.graphql`
# (and `setup/graphql/operations/login.variables(.local).json` if present) to fetch a token.

GQL_JWT_DEFAULT=
GQL_JWT_ADMIN=
GQL_JWT_MEMBER=
