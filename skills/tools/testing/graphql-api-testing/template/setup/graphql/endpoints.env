# GraphQL endpoint presets (template).
# Used by: $CODEX_HOME/skills/tools/testing/graphql-api-testing/scripts/gql.sh
#
# Notes:
# - This file is safe to commit (no secrets).
# - For per-developer overrides (e.g. different local port), create:
#     setup/graphql/endpoints.local.env
#   (and keep it uncommitted / gitignored).

GQL_ENV_DEFAULT=local

# Local dev
GQL_URL_LOCAL=http://localhost:6700/graphql

# Remote environments (fill in if applicable)
# GQL_URL_DEV=https://<dev-host>/graphql
# GQL_URL_STAGING=https://<staging-host>/graphql
