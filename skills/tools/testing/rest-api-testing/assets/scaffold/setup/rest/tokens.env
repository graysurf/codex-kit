# Bearer token presets (no secrets in this file).
#
# Put real tokens in `setup/rest/tokens.local.env` (gitignored via setup/rest/*.local.env).
# Select which one to use:
#   - CLI: api-rest call --token <name> ... (from `brew install nils-cli`)
#   - Env: export REST_TOKEN_NAME="<name>"
#   - Or (local-only): set `REST_TOKEN_NAME=<name>` inside `setup/rest/tokens.local.env`
#
# Naming convention:
#   REST_TOKEN_DEFAULT   -> name "default"
#   REST_TOKEN_ADMIN     -> name "admin"
#   REST_TOKEN_MEMBER    -> name "member"

REST_TOKEN_DEFAULT=
REST_TOKEN_ADMIN=
REST_TOKEN_MEMBER=
