# REST endpoint presets (template).
# Used by: $CODEX_HOME/skills/tools/testing/rest-api-testing/bin/api-rest
#
# Notes:
# - This file is safe to commit (no secrets).
# - For per-developer overrides (e.g. different local port), create:
#     setup/rest/endpoints.local.env
#   (and keep it uncommitted / gitignored).

REST_ENV_DEFAULT=local

# Local dev
REST_URL_LOCAL=http://localhost:6700

# Remote environments (fill in if applicable)
# REST_URL_DEV=https://<dev-host>
# REST_URL_STAGING=https://<staging-host>
