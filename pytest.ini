[pytest]
addopts = -ra
norecursedirs =
  .git
  .worktrees
  .venv
  out
  tmp
markers =
  script_regression: run repo script entrypoints in a hermetic harness
  script_smoke: run deeper smoke cases for selected scripts
